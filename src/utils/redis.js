// const redis = require("redis");
// const createRedisClient = async (
//   config = { host: process.env.DB_HOST, port: 6379 }
// ) => {
//   const client = redis.createClient(config);
//   await client.connect();
//   return client;
// };
// module.exports = { createRedisClient };
